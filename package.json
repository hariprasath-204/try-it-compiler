{
  "name": "omni-compiler",
  "version": "1.0.0",
  "description": "Omni-Compiler Root",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:images": "docker compose build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
